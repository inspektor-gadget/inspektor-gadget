version: 2
jobs:
 build:
   docker:
     - image: circleci/golang:1.12
   working_directory: /go/src/github.com/kinvolk/inspektor-gadget
   steps:
     - checkout

     - restore_cache:
         keys:
           - go-mod-v1-{{ checksum "go.sum" }}

     - setup_remote_docker:
         docker_layer_caching: true

     - run: |
         echo "$DOCKER_PASS" | docker login --username $DOCKER_USER --password-stdin

     # build the application
     - run: make
     - run: make -C gadget-ds/ build

     - save_cache:
         key: go-mod-v1-{{ checksum "go.sum" }}
         paths:
           - "/go/pkg/mod"

     # deploy the image
     - run: make -C gadget-ds/ push
