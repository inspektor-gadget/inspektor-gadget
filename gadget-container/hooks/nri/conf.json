{
    "version": "0.1",
    "plugins": [
        {
            "type": "{{.namespace}}-nrigadget",
            "conf": {
                "socketFile": "/run/gadgettracermanager.socket"
            }
        }
    ]
}
