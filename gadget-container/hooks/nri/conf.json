{
    "version": "0.1",
    "plugins": [
        {
            "type": "{{.namespace}}-nrigadget",
            "conf": {
                "socketFile": "/run/{{.namespace}}-gadgettracermanager.socket"
            }
        }
    ]
}
