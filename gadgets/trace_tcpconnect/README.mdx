---
title: trace_tcpconnect
sidebar_position: 0
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# trace_tcpconnect

The gadget was merged into the `trace_tcp` gadget in version `v0.37.0`.
The `trace_tcp` gadget can be instrumented to only show `connect` events if no other events are needed.
